// if () {} else {}

const n = 37;

if (n > 0) {
    console.log('n이 0보다 큰 경우');
} else {
    console.log('n이 0보다 크지 않은 경우, 이하');
}

// 블록 안 문장이 하나일 경우, 중괄호 생략가능

if (n > 0) console.log('n이 0보다 큰 경우');
else console.log('n이 0보다 크지 않은 경우, 이하');